import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bc2ip.tools',
  appName: 'bc2ip Tools',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
